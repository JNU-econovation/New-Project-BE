spring:
  application:
    name: NewProject
  config:
    import: optional:file:.env[.properties]
  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT}
      password: ${REDIS_PASSWORD}
      ssl:
        enabled: true
  profiles:
    group:
      local:
        - jpa
        - token
        - kakao
        - apple
