spring:
  application:
    name: NewProject
  config:
    import: optional:file:.env[.properties], optional:file:metrics.yml
  jackson:
    property-naming-strategy: SNAKE_CASE

  profiles:
    group:
      local:
        - jpa
        - token
        - kakao