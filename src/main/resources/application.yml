spring:
  application:
    name: NewProject
  config:
    import: optional:file:.env[.properties]
  profiles:
    group:
      local:
        - jpa
        - token
        - kakao
        - apple
        - metrics